spring.application.name=foomyfood

# Spring Boot mail setting
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# The email address of the sender
spring.mail.username=foomyfood@gmail.com
# The sender's password or application-specific password (if two-step verification is enabled)
spring.mail.password=wjhh qmeo mupr jpqn
# Enable TLS encryption
spring.mail.properties.mail.smtp.starttls.enable=true
# Verify the identity of the mail server
spring.mail.properties.mail.smtp.auth=true
# Mail Transfer Protocol
spring.mail.properties.mail.transport.protocol=smtp

# Database connection
spring.datasource.url=jdbc:mysql://34.170.115.186:3306/cs307_project?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=test
spring.datasource.password=test
server.port=8081
com.sun.management.jmxremote.port=61024
com.sun.management.jmxremote.ssl=false
com.sun.management.jmxremote.authenticate=false

# Connection pool
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# GCP id setting
spring.cloud.gcp.project-id=foo-my-food-cs307

# JPA properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
# spring.sql.init.mode=always
# spring.sql.init.data-locations=classpath:PresetFood.sql

# GCS
gcs.bucket-name=foo_my_food_user_image
server.address=0.0.0.0

# API
server.servlet.session.timeout=30m
